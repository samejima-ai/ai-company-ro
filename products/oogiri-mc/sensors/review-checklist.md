# 独立検証チェックリスト（プロジェクト固有） — ヤジ

layer1-independent-reviewer が実装コンテキストから隔離した状態で参照する、ヤジ固有の検証観点。
汎用観点（型・ビルド・テスト・SPEC 合致）は sensors/computational.md / inferential.md を使う。本ファイルは固有項目のみ。

## ガバナンス境界
- [ ] persona 文字列が `personas/riko_system_prompt.md` の 1 箇所にのみ存在し、`src/` に二重定義されていない。
- [ ] persona・NG・モデレーション辞書の変更が、コード内の自動改変経路ではなく、設定/データファイル＋人間ゲート（PR）でのみ反映される構造になっている。
- [ ] 保護ブランチへの直接 push 経路がない（変更は PR のみ）。

## 安全二重化
- [ ] 入口モデレーションが「モデルに渡す前」に走る（インジェクション・未成年疑い・NGトピック）。
- [ ] 出口モデレーションが「送信前」に走り、FAIL 時は再生成1回→なお FAIL なら安全定型に差し替え。
- [ ] モデレーション機構自体の失敗が「通さない（送信中止＋ログ）」側に倒れている（fail-closed）。
- [ ] DONT.md ハード制約と反面教師手口が、コード側検査（src/moderation）と spec/invariants.feature Evil Path の双方でカバーされている（片方だけでない）。

## 記憶の用途制限
- [ ] `confession`（打ち明け話）kind が F2 の context 組み立てで除外されている。
- [ ] 記憶上限超過時に `confession` が保持優先される。
- [ ] ユーザー削除要求で profile/memory/log が同一トランザクションで物理削除される。

## Phase 境界
- [ ] 投げ銭・決済・SKU が Phase 0 で起動しない（席=インターフェースのみ、発火経路なし）。
- [ ] eval 目的関数が投げ銭単独になっていない（複合: 視聴維持＋安全違反ゼロ）。

## 秘密情報
- [ ] `.env` が追跡されておらず、`.env.example` に実値が入っていない。
- [ ] ログにユーザー識別子として保存されるのは Discord user ID のみ（氏名・連絡先なし）。
